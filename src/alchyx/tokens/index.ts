/**
 * @alchyx/tokens — typed, programmatic access to the D-ALabs design language.
 * Re-exported here so component code can `import { ... } from "@alchyx/tokens"`
 * exactly as in the authoring contract.
 */
export * from "./tokens";
export { default } from "./tokens";
